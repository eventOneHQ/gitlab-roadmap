<div class="container">
    <div class="row">
        <div class="col">
            <h1>{{name}}</h1>
            <p>{{message}}</p>

            {{#if stack}}
            <code>
                {{stack}}
            </code> {{/if}}
        </div>
    </div>
</div>